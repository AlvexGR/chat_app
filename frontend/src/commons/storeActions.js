const toaster = {
  set: "toaster.set",
};

const auth = {
  set: "auth.set",
};

const chat = {
  invoke: "chat.invoke",
  receive: "chat.receive",
};

const notification = {};

export const storeActions = {
  toaster,
  auth,
  chat,
  notification,
};
