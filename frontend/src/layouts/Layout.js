import MainContent from "./MainContent"
import SideBar from "./SideBar"

const Layout = () => {

  return (
    <div className="row">
      <SideBar />
      <MainContent />
    </div>
  )
}

export default Layout
